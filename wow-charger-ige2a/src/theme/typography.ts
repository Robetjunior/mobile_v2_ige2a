import { TextStyle } from 'react-native';

export const Typography = {
  titleSection: { fontSize: 22, fontWeight: '600' } as TextStyle,
  userName: { fontSize: 26, fontWeight: '600' } as TextStyle,
  subId: { fontSize: 15, fontWeight: '500' } as TextStyle,
  itemLabel: { fontSize: 17, fontWeight: '600' } as TextStyle,
  body: { fontSize: 16, fontWeight: '500' } as TextStyle,
} as const;