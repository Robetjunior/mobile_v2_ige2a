# Expo App (.env.example)
# Copy to .env and fill the values. Expo will expose EXPO_PUBLIC_* to the app.

# ===== API (Orchestrator) =====
# Base URL for backend. Keep without trailing slash.
EXPO_PUBLIC_API_BASE_URL=http://35.231.137.231:3000
# API key for protected /v1/** routes.
EXPO_PUBLIC_API_KEY=your-dev-api-key

# ===== Supabase Auth =====
# Public URL from Supabase project settings
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
# Public anon key from Supabase project settings
EXPO_PUBLIC_SUPABASE_ANON_KEY=YOUR-ANON-KEY

# ===== Deep Link =====
# Web preview port; update if you use a custom port
EXPO_PUBLIC_WEB_PORT=8084

# Notes:
# - On web OAuth, redirect will target http://localhost:${EXPO_PUBLIC_WEB_PORT}/auth-callback
# - On native OAuth, redirect uses ev://auth-callback
# - For development without Supabase configured, the app will warn and use placeholders (no real auth).